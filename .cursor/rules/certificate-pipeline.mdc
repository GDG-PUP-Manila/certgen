---
description: Certificate generation pipeline (Satori, Resvg, PDFKit)
globs: src/services/**,src/pages/api/generate-cert.ts,public/templates/**,test/test-pdf.ts
alwaysApply: false
---

# Certificate Pipeline

Template layout comes from `survey.cert_config` JSONB (Admin Visual Designer), resolved in `certificateWorkflow.service.ts`:

- `template_url` / `templateFilename`
- `text_top_offset`
- `text_color`
- `text_font_size`

Do **not** add per-slug `if` branches for template selection.

## Dimensions

- Satori canvas: **1000 × 707** (A4 landscape ratio)
- Resvg output: **3000px** wide, transparent background
- PDFKit page: A4 landscape (**841.89 × 595.28 pt**)
- Only **participant name** is dynamic; rest is baked into JPG template
- Names truncated to **40 chars** before render

## New event template

1. Export Canva → PNG → optimize to `public/templates/{slug}-optimized.jpg` (MozJPEG ~90)
2. Set `cert_config` via Admin Visual Designer at `/admin/events/{event_id}/survey`
3. Tune with `npm run test:pdf` → inspect `test/output/test-output.pdf`

```bash
node -e "const sharp=require('sharp'); sharp('public/templates/my-cert.png').jpeg({ quality: 90, mozjpeg: true }).toFile('public/templates/my-event-optimized.jpg').then(console.log);"
```

## Defaults (when `cert_config` empty)

| Field | Default |
|-------|---------|
| template | `base-template-optimized.jpg` |
| `text_top_offset` | `290px` |
| `text_color` | `#1e293b` |
| `text_font_size` | `50px` |

Historical seed values for older surveys: `docs/sql/seeds/DYNAMIC_SEED.sql`.
